.env
src/.env
src/prod_creds.yaml
src/deployment/docker.txt
src/deployment/.env

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# password, private keys, and other secrets. Alternatively, you could
# only ignore specific .tfvars files with sensitive data.
*.tfvars

# Ignore override files as they are usually used for local terraform commands
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
